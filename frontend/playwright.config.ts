import { defineConfig, devices } from "@playwright/test";

/** E2E runs against the Vite build in a real browser — the UI logic doesn't
 * depend on the native shell (docs/testing-strategy.md §4). The backend API must
 * be up at API_BASE_URL with a known password (E2E_PASSWORD). */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI ? "line" : "list",
  use: {
    baseURL: "http://localhost:1420",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:1420",
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
